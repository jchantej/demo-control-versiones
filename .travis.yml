language: java
jdk:
- oraclejdk8
branches:
  only:
  - master
  - develop
notifications:
  email:
    recipients:
    - jchantej@gmail.com
script:
#Test unitario y de integracion en develop
- mvn verify --settings settings.xml
# ============ mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent verify --settings settings.xml ... ==================
# Se prepara cobertura
- mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent verify --settings settings.xml

# ============ mvn sonar:sonar ... =======================================================
#  Se analiza y sube a sonar cloud
- mvn sonar:sonar -Dsonar.organization=jchantej-github -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=5764a3c111aa839db2183baebf4311399ada17b1 --settings settings.xml
#============ mvn package --settings settings.xml  (profile: preproduction) =====================================

- mvn verify -Denvironment.type=preproduction --settings settings.xml
